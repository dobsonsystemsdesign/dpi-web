import { ScrollStatusDirective } from './scroll-status.directive';

describe('ScrollStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollStatusDirective();
    expect(directive).toBeTruthy();
  });
});
